# Dependencies and build output
node_modules
.next
out
build
coverage

# Generated by `next typegen`
next-env.d.ts

# Lokalise owns these — see docs/translations.md. Reformatting them would put
# this repository's whitespace preferences into files a different tool writes.
src/locales

# Lockfile
package-lock.json

# Helm templates are Go templates that happen to end in .yaml — Prettier's YAML
# parser cannot read them, and reformatting the chart is not this repository's
# job anyway.
helm/*/templates
