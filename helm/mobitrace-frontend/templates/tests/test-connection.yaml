apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mobitrace-frontend.fullname" . }}-test-connection"
  labels:
    {{- include "mobitrace-frontend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mobitrace-frontend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
